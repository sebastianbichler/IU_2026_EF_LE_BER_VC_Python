[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "foxpost"
version = "0.1.0"
description = "FoxPost Paketdaten Pipeline mit Dask"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]